{
  "name": "cdk",
  "version": "0.1.0",
  "bin": {
    "cdk": "bin/cdk.js"
  },
  "scripts": {
    "build": "npm run clean && npm run lint && echo Using TypeScript && tsc --version && tsc --pretty --project tsconfig-publish.json && cp -r package.json README.md cdk/resources lib/",
    "clean": "rimraf cdk.out lib",
    "deploy": "npm run clean && NAMESPACE=$(whoami | tr '.' '-') cdk deploy",
    "destroy:dev": "npm run clean && NAMESPACE=dev cdk destroy \"*\"",
    "destroy": "npm run clean && NAMESPACE=$(whoami | tr '.' '-') cdk destroy \"*\"",
    "diff": "npm run clean && NAMESPACE=$(whoami | tr '.' '-') cdk diff",
    "info": "npm info @naviance/devstack@$npm_package_version",
    "lint": "eslint . --ext=.js,.ts",
    "pretest": "npm run lint",
    "synth": "npm run clean && NAMESPACE=$(whoami | tr '.' '-') cdk synth",
    "test": "jest --coverage --runInBand --silent",
    "build-orig": "tsc",
    "watch": "tsc -w",
    "test-orig": "jest",
    "cdk": "cdk"
  },
  "devDependencies": {
    "@types/jest": "^26.0.10",
    "@types/node": "10.17.27",
    "aws-cdk": "2.12.0",
    "jest": "^26.4.2",
    "serverless": "^3.15.2",
    "serverless-plugin-log-retention": "^2.0.0",
    "ts-jest": "^26.2.0",
    "ts-node": "^9.0.0",
    "typescript": "~3.9.7"
  },
  "dependencies": {
    "aws-cdk-lib": "2.12.0",
    "constructs": "^10.0.0",
    "source-map-support": "^0.5.16"
  }
}
